generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["attendance", "bank_validation", "field_support", "public"]
}

/// This table has subclasses and requires additional setup for migrations. Visit https://pris.ly/d/table-inheritance for more info.
/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model lga_supervisor_application {
  name                 String
  lga                  String?
  ward                 String?
  latitude             String
  longitude            String
  total_points         BigInt
  submission_time      DateTime? @db.Timestamptz(6)
  phone_number         String    @unique(map: "unique_lga_phone")
  email                String?
  gender               String
  school_qualification String
  hausa_fluency        String
  english_fluency      String
  full_coordinates     String?
  status               String?
  dob                  DateTime? @db.Date
  id                   String    @id @unique @db.Uuid
  device               String
  valid_bank           String
  years_experience     String

  @@schema("public")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ward_supervisor_application {
  name                 String
  lga                  String
  ward                 String
  latitude             String
  longitude            String
  phone_number         String    @unique(map: "unique_ward_phone")
  email                String?
  gender               String
  school_qualification String
  hausa_fluency        String
  english_fluency      String
  full_coordinates     String?
  status               String?
  dob                  DateTime? @db.Date
  device               String
  total_points         BigInt
  submission_time      DateTime? @db.Timestamptz(6)
  id                   String    @id @unique @db.Uuid
  valid_bank           String
  years_experience     String

  @@schema("public")
}

model data_clerk_application {
  id                   String    @id @unique @db.Uuid
  name                 String
  lga                  String
  ward                 String
  latitude             String
  longitude            String
  phone_number         String    @unique(map: "unique_clerk_phone")
  email                String?
  gender               String
  school_qualification String
  hausa_fluency        String
  english_fluency      String
  full_coordinates     String?
  status               String?
  dob                  DateTime? @db.Date
  device               String
  total_points         BigInt
  submission_time      DateTime? @db.Timestamptz(6)
  valid_bank           String
  years_experience     String

  @@schema("public")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model shortlisted_applicant_form {
  firstname                           String
  lastname                            String
  dob                                 String
  phone_number                        String   @unique(map: "phone_number_unique")
  email                               String?
  lga                                 String
  ward                                String
  full_address                        String
  willing_to_be_reassigned            String?
  id_type                             String
  name_of_bank                        String
  bank_acct_name                      String
  bank_acct_no                        String
  id                                  String   @id @unique @db.Uuid
  created_at                          DateTime @db.Timestamptz(6)
  gender                              String
  file_name                           String
  first_choice_ward_for_redeployment  String?
  second_choice_ward_for_redeployment String?
  willing_to_be_redeployed            String
  middlename                          String?
  initial_role                        String
  whatsapp                            String?

  @@schema("public")
}

model lga_assistant_20250403 {
  id             String   @id @db.Uuid
  firstname      String
  lastname       String
  middlename     String?
  dob            String
  phone_number   String   @unique
  email          String?
  lga            String
  ward           String
  full_address   String
  id_type        String
  name_of_bank   String
  bank_acct_name String
  bank_acct_no   String
  created_at     DateTime @db.Timestamptz(6)
  gender         String
  file_name      String
  whatsapp       String?
  network        String

  @@schema("attendance")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ajingi {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  ajingi         attendance_ajingi[]

  @@index([phone_number], map: "idx_ajingi_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sumaila {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String               @id @unique
  phone_number   String               @unique
  sumaila        attendance_sumaila[]

  @@index([account_number], map: "idx_sumaila_account_number")
  @@index([phone_number], map: "idx_sumaila_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tsanyawa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                @id
  phone_number   String                @unique
  tsanyawa       attendance_tsanyawa[]

  @@index([phone_number], map: "idx_tsanyawa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model madobi {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  madobi         attendance_madobi[]

  @@index([phone_number], map: "idx_madobi_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bagwai {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  bagwai         attendance_bagwai[]

  @@index([phone_number], map: "idx_bagwai_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gaya {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  gaya           attendance_gaya[]

  @@index([phone_number], map: "idx_gaya_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kumbotso {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                @id
  phone_number   String                @unique
  kumbotso       attendance_kumbotso[]

  @@index([phone_number], map: "idx_kumbotso_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gezawa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  gezawa         attendance_gezawa[]

  @@index([phone_number], map: "idx_gezawa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model makoda {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  makoda         attendance_makoda[]

  @@index([phone_number], map: "idx_makoda_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kunchi {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String?
  kunchi         attendance_kunchi[]

  @@index([phone_number], map: "idx_kunchi_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tudun_wada {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                  @id
  phone_number   String                  @unique
  tudun_wada     attendance_tudun_wada[]

  @@index([phone_number], map: "idx_tudunwada_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tofa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  tofa           attendance_tofa[]

  @@index([phone_number], map: "idx_tofa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model fagge {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  fagge          attendance_fagge[]

  @@index([phone_number], map: "idx_fagge_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gabasawa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                @id
  phone_number   String                @unique
  gabasawa       attendance_gabasawa[]

  @@index([phone_number], map: "idx_gabasawa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gwale {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  gwale          attendance_gwale[]

  @@index([phone_number], map: "idx_gwale_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model doguwa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  doguwa         attendance_doguwa[]

  @@index([phone_number], map: "idx_doguwa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bichi {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  bichi          attendance_bichi[]

  @@index([phone_number], map: "idx_bichi_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model gwarzo {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  gwarzo         attendance_gwarzo[]

  @@index([phone_number], map: "idx_gwarzo_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kura {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  kura           attendance_kura[]

  @@index([phone_number], map: "idx_kura_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kibiya {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  kibiya         attendance_kibiya[]

  @@index([phone_number], map: "idx_kibiya_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kiru {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  kiru           attendance_kiru[]

  @@index([phone_number], map: "idx_kiru_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kumbotso_lg {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String @id
  phone_number   String @unique

  @@index([phone_number], map: "idx_kumbotsolg_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model garko {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  garko          attendance_garko[]

  @@index([phone_number], map: "idx_garko_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tarauni {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String   @id
  phone_number   String   @unique
  taruni         taruni[]

  @@index([phone_number], map: "idx_tarauni_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model dambatta {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                @id
  phone_number   String                @unique
  dambatta       attendance_dambatta[]

  @@index([phone_number], map: "idx_dambatta_phone_number")
  @@schema("bank_validation")
}

model complaints {
  lga          String
  ward         String
  organization String
  name         String
  phone_number String
  type         String?
  problem      String?
  date         String
  id           String  @id @db.Uuid
  settlement   String

  @@schema("field_support")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model dawakin_tofa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                    @id
  phone_number   String                    @unique
  dawakin_tofa   attendance_dawakin_tofa[]

  @@index([phone_number], map: "idx_dtofa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model garun_malam {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                   @id
  phone_number   String                   @unique
  garun_malam    attendance_garun_malam[]

  @@index([phone_number], map: "idx_gmalam_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kabo {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  kabo           attendance_kabo[]

  @@index([phone_number], map: "idx_kabo_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model nassarawa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                 @id
  phone_number   String                 @unique
  nassarawa      attendance_nassarawa[]

  @@index([phone_number], map: "idx_nassarawa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model shanono {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String               @id
  phone_number   String               @unique
  shanono        attendance_shanono[]

  @@index([phone_number], map: "idx_shanono_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bebeji {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  bebeji         attendance_bebeji[]

  @@index([phone_number], map: "idx_bebeji_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model albasu {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  albasu         attendance_albasu[]

  @@index([phone_number], map: "idx_albasu_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rimin_gado {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                  @id
  phone_number   String                  @unique
  rimin_gado     attendance_rimin_gado[]

  @@index([phone_number], map: "idx_rgado_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model dala {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  dala           attendance_dala[]

  @@index([phone_number], map: "idx_dala_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rogo {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  rogo           attendance_rogo[]

  @@index([phone_number], map: "idx_rogo_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model dawakin_kudu {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                    @id
  phone_number   String                    @unique
  dawakin_kudu   attendance_dawakin_kudu[]

  @@index([phone_number], map: "idx_dkudu_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model minjibir {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                @id
  phone_number   String                @unique
  minjibir       attendance_minjibir[]

  @@index([phone_number], map: "idx_minjibir_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model rano {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String            @id
  phone_number   String            @unique
  rano           attendance_rano[]

  @@index([phone_number], map: "idx_rano_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model wudil {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  wudil          attendance_wudil[]

  @@index([phone_number], map: "idx_wudil_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model warawa {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  warawa         attendance_warawa[]

  @@index([phone_number], map: "idx_warawa_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bunkure {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String               @id
  phone_number   String               @unique
  bunkure        attendance_bunkure[]

  @@index([phone_number], map: "idx_bunkure_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model kano_municipal {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String                      @id
  phone_number   String                      @unique
  kano_municipal attendance_kano_municipal[]

  @@index([phone_number], map: "idx_kmc_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model takai {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String             @id
  phone_number   String             @unique
  takai          attendance_takai[]

  @@index([phone_number], map: "idx_takai_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model karaye {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  karaye         attendance_karaye[]

  @@index([phone_number], map: "idx_karaye_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ungogo {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String              @id
  phone_number   String              @unique
  ungogo         attendance_ungogo[]

  @@index([phone_number], map: "idx_ungogo_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ungogo_lg {
  name           String
  ward           String
  designation    String
  bank_name      String
  bank_code      String
  account_number String @id
  phone_number   String @unique

  @@index([phone_number], map: "idx_ungogo_lg_phone_number")
  @@schema("bank_validation")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_ajingi {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  ajingi          ajingi   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("ajingi")
  @@schema("attendance")
}

model attendance_albasu {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  albasu          albasu   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("albasu")
  @@schema("attendance")
}

model attendance_bagwai {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  bagwai          bagwai   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("bagwai")
  @@schema("attendance")
}

model attendance_bebeji {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  bebeji          bebeji   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("bebeji")
  @@schema("attendance")
}

model attendance_bichi {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  bichi           bichi    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("bichi")
  @@schema("attendance")
}

model attendance_bunkure {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  bunkure         bunkure  @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("bunkure")
  @@schema("attendance")
}

model attendance_dala {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  dala            dala     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("dala")
  @@schema("attendance")
}

model attendance_dambatta {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  dambatta        dambatta @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("dambatta")
  @@schema("attendance")
}

model attendance_dawakin_kudu {
  id              String       @id @db.Uuid
  created_at      DateTime     @db.Date
  account_number  String
  attendance_date String
  dawakin_kudu    dawakin_kudu @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("dawakin_kudu")
  @@schema("attendance")
}

model attendance_dawakin_tofa {
  id              String       @id @db.Uuid
  created_at      DateTime     @db.Date
  account_number  String
  attendance_date String
  dawakin_tofa    dawakin_tofa @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("dawakin_tofa")
  @@schema("attendance")
}

model attendance_doguwa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  doguwa          doguwa   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("doguwa")
  @@schema("attendance")
}

model attendance_fagge {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  fagge           fagge    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("fagge")
  @@schema("attendance")
}

model attendance_gabasawa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  gabasawa        gabasawa @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("gabasawa")
  @@schema("attendance")
}

model attendance_garko {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  garko           garko    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("garko")
  @@schema("attendance")
}

model attendance_garun_malam {
  id              String      @id @db.Uuid
  created_at      DateTime    @db.Date
  account_number  String
  attendance_date String
  garun_malam     garun_malam @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("garun_malam")
  @@schema("attendance")
}

model attendance_gaya {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  gaya            gaya     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("gaya")
  @@schema("attendance")
}

model attendance_gezawa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  gezawa          gezawa   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("gezawa")
  @@schema("attendance")
}

model attendance_gwale {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  gwale           gwale    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("gwale")
  @@schema("attendance")
}

model attendance_gwarzo {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  gwarzo          gwarzo   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("gwarzo")
  @@schema("attendance")
}

model attendance_kabo {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kabo            kabo     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kabo")
  @@schema("attendance")
}

model attendance_kano_municipal {
  id              String         @id @db.Uuid
  created_at      DateTime       @db.Date
  account_number  String
  attendance_date String
  kano_municipal  kano_municipal @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kano_municipal")
  @@schema("attendance")
}

model attendance_karaye {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  karaye          karaye   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("karaye")
  @@schema("attendance")
}

model attendance_kibiya {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kibiya          kibiya   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kibiya")
  @@schema("attendance")
}

model attendance_kiru {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kiru            kiru     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kiru")
  @@schema("attendance")
}

model attendance_kumbotso {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kumbotso        kumbotso @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kumbotso")
  @@schema("attendance")
}

model attendance_kunchi {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kunchi          kunchi   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kunchi")
  @@schema("attendance")
}

model attendance_kura {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  kura            kura     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("kura")
  @@schema("attendance")
}

model attendance_madobi {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  madobi          madobi   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("madobi")
  @@schema("attendance")
}

model attendance_makoda {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  makoda          makoda   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("makoda")
  @@schema("attendance")
}

model attendance_minjibir {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  minjibir        minjibir @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("minjibir")
  @@schema("attendance")
}

model attendance_nassarawa {
  id              String    @id @db.Uuid
  created_at      DateTime  @db.Date
  account_number  String
  attendance_date String
  nassarawa       nassarawa @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("nassarawa")
  @@schema("attendance")
}

model attendance_rano {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  rano            rano     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("rano")
  @@schema("attendance")
}

model attendance_rimin_gado {
  id              String     @id @db.Uuid
  created_at      DateTime   @db.Date
  account_number  String
  attendance_date String
  rimin_gado      rimin_gado @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("rimin_gado")
  @@schema("attendance")
}

model attendance_rogo {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  rogo            rogo     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("rogo")
  @@schema("attendance")
}

model attendance_shanono {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  shanono         shanono  @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("shanono")
  @@schema("attendance")
}

model attendance_sumaila {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  sumaila         sumaila  @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("sumaila")
  @@schema("attendance")
}

model attendance_takai {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  takai           takai    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("takai")
  @@schema("attendance")
}

model attendance_tofa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  tofa            tofa     @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("tofa")
  @@schema("attendance")
}

model attendance_tsanyawa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  tsanyawa        tsanyawa @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("tsanyawa")
  @@schema("attendance")
}

model attendance_tudun_wada {
  id              String     @id @db.Uuid
  created_at      DateTime   @db.Date
  account_number  String
  attendance_date String
  tudun_wada      tudun_wada @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("tudun_wada")
  @@schema("attendance")
}

model attendance_ungogo {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  ungogo          ungogo   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("ungogo")
  @@schema("attendance")
}

model attendance_warawa {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  warawa          warawa   @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("warawa")
  @@schema("attendance")
}

model attendance_wudil {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  wudil           wudil    @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@map("wudil")
  @@schema("attendance")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model new_staff {
  id             String   @id @db.Uuid
  lga            String
  ward           String
  name           String
  phone_number   String
  account_number String   @unique(map: "unique_account_number")
  bank_name      String
  created_at     DateTime @db.Date
  designation    String

  @@schema("attendance")
}

model taruni {
  id              String   @id @db.Uuid
  created_at      DateTime @db.Date
  account_number  String
  attendance_date String
  tarauni         tarauni  @relation(fields: [account_number], references: [account_number], onDelete: Cascade, onUpdate: NoAction)

  @@unique([account_number, attendance_date])
  @@schema("attendance")
}

model dataclerk_qualified_applicant {
  name         String
  phone_number String  @unique

  // Maps to the PostgreSQL materialized view
  @@map("dataclerk_qualified_applicant")

  @@schema("public")
}

model lga_qualified_applicant {
  name         String
  phone_number String  @unique

  // Maps to the PostgreSQL materialized view
  @@map("lga_qualified_applicant")

  @@schema("public")
}

model ward_qualified_applicant {
  name         String
  phone_number String  @unique

  // Maps to the PostgreSQL materialized view
  @@map("ward_qualified_applicant")

  @@schema("public")
}
